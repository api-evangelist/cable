openapi: 3.2.0
info:
  title: Customer Data Helper API
  version: 1.0.0
servers:
- url: https://api.cable.tech
  description: Placeholder server
tags:
- name: helper
paths:
  /status:
    get:
      operationId: status-check
      summary: Organization status check
      description: Endpoint for organization status check purposes
      tags:
      - helper
      parameters:
      - name: x-api-key
        in: header
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Successful response
components:
  securitySchemes:
    isValidAPIKey:
      type: apiKey
      in: header
      name: x-api-key
